version: '3'

tasks:
  install:
    desc: Install JS dependencies
    cmds:
    - npm install

  install-ci:
    cmds:
    - npm ci

  dev:
    desc: Run RDB in dev (watch) mode
    cmds:
    - npx tauri dev

  build-js:
    desc: Build RDB Frontend
    cmds:
    - npx tsc && npx vite build